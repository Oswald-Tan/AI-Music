import 'package:flutter/material.dart';

class AppColors {
  // AMOLED / Pure Black Palette
  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF0A0A0A);
  static const Color surfaceLight = Color(0xFF111111);
  static const Color surfaceLighter = Color(0xFF1A1A1A);
  
  // Accents
  static const Color primary = Color(0xFF8B5CF6); // Electric Purple
  static const Color primaryLight = Color(0xFFA78BFA); // Violet
  static const Color secondary = Color(0xFF06B6D4); // Cyan
  
  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0A0);
  static const Color textMuted = Color(0xFF666666);
  
  // States
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color border = Color(0xFF1F1F1F);
}
